---
interface Props {
  class?: string;
  eager?: boolean;
}

const { class: className = "", eager = false } = Astro.props;
const loadClass = eager ? "brand-switch--eager" : "";
---

<div class:list={["brand-switch", loadClass, className]} aria-hidden="true">
</div>
