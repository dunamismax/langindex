services:
  site:
    build:
      context: .
    image: langindex:local
    environment:
      SITE_ADDRESS: ":80"
    ports:
      - "127.0.0.1:${LANGINDEX_HOST_PORT:-8090}:80"
    restart: unless-stopped
